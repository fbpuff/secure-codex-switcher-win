# Secure Codex Switcher 2.16.11

## 源码恢复说明

- 生产源码以正式安装包内的 `2.16.11` ASAR 为基准恢复；该正式包记录的来源为分支 `codex/complete-branch-integration`、提交 `ab1b259e866b7cfb2af8658a6ccafe5381348120`、构建时间 `2026-08-28T17:15:23.8834366Z`。
- 原提交对象当前不在恢复后的本地 Git 对象库中，因此上述记录用于说明生产源码来源，不代表原始 Git 历史已经完整恢复。

## 新功能

- 新增“添加新账号”入口：先将当前登录凭据刷新并以 DPAPI 加密保存到 Switcher，再在确认 Codex 已完全关闭且线程状态稳定后移除活动凭据，让用户登录新账号。
- 原账号继续保存在 Switcher 中；新账号登录完成后可通过现有导入流程保存，无需先从 Switcher 删除或退出原账号。
- 如果移除活动凭据后的线程复核失败，并且原凭据也无法安全写回，则采取失败关闭策略：停止添加流程、不重启 Codex，并保留 Switcher 中已保存的账号记录。

## 改进

- 拖动实时额度窗靠近任一屏幕边缘时自动吸附，放宽原生鼠标拖动的释放容差至 64px。
- 保留现有左右切换按钮作为备用入口。
- 点击 5 小时或 7 天额度球时，按当前账号与当前额度周期显示可归属的本地 token 消耗；不混入其他账号。
- 切换账号后清空上一账号的额度详情缓存，避免跨账号显示旧 token 数值。
- 最近完成项目成功打开 Codex 后标记为已读，并从该账号的实时窗列表隐藏。

## 修复

- 修复 Windows PowerShell 将 CIM 启动信息传给 `Win32_Process.Create` 时发生类型不匹配、导致 watchdog 实际未启动的问题；改用兼容的 WMI 嵌入实例，保持后台无窗口启动。
- `turn/start` 已发送但响应丢失时保留为 `uncertain`，不再错误写成 `failed`；避免掩盖可能已经启动的续接，也避免盲目重复提交。
- 修复 app-server JSON-RPC 将带 `id` 的服务端请求误当作客户端响应的问题；只有不含 `method` 的消息才会结算待处理请求。
- 当前账号刷新完成后再并发刷新其他账号，避免刷新顺序竞态。
- 切换前捕获稳定线程状态，并跳过空或损坏的 rollout 文件，同时返回诊断信息。
