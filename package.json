{
  "name": "secure-codex-switcher-win",
  "productName": "Secure Codex Switcher",
  "version": "2.19.8",
  "private": true,
  "author": "Secure Codex Switcher Contributors",
  "description": "A local-only Windows Codex account switcher with encrypted account storage and usage-aware switching.",
  "type": "module",
  "main": "src/main.js",
  "engines": {
    "node": ">=22.19.0"
  },
  "scripts": {
    "start": "electron .",
    "package:dir": "pwsh -NoProfile -File scripts/package-formal.ps1 -DirectoryOnly",
    "package:win": "pwsh -NoProfile -File scripts/package-formal.ps1",
    "package:public": "electron-builder --win nsis --x64 --publish never",
    "verify:formal-install": "pwsh -NoProfile -File scripts/verify-formal-install.ps1",
    "test": "node --test tests/*.test.js",
    "audit:prod": "npm audit --omit=dev"
  },
  "dependencies": {
    "off": "^0.0.10",
    "undici": "^8.5.0"
  },
  "devDependencies": {
    "electron": "^42.6.1",
    "electron-builder": "^26.0.12"
  },
  "build": {
    "appId": "com.securecodexswitcher.windows",
    "productName": "Secure Codex Switcher",
    "asar": true,
    "artifactName": "${productName}-${version}-${arch}.${ext}",
    "directories": {
      "output": "dist",
      "buildResources": "build"
    },
    "files": [
      "src/**/*",
      "build/icon.png",
      "package.json"
    ],
    "extraResources": [
      {
        "from": "scripts/Watch-SwitcherProcess.ps1",
        "to": "Watch-SwitcherProcess.ps1"
      },
      {
        "from": "build/shortcut-icon-2.5.2.ico",
        "to": "shortcut-icon-2.5.2.ico"
      },
      {
        "from": "build/tray-icon-2.5.3.png",
        "to": "tray-icon-2.5.3.png"
      }
    ],
    "win": {
      "target": "nsis",
      "executableName": "Secure Codex Switcher",
      "icon": "build/icon.ico"
    },
    "nsis": {
      "oneClick": false,
      "allowToChangeInstallationDirectory": true,
      "createDesktopShortcut": true,
      "createStartMenuShortcut": true,
      "shortcutName": "Codex Switcher",
      "deleteAppDataOnUninstall": false
    }
  }
}
